# @package _global_

defaults:
  - override /datamodule: moses

datamodule:
  batch_size: 512

sampling:
  top_k: 5

model:
  model_name: llama-s
  pretrained_path: pretrained_models/train/MOSES.ckpt
