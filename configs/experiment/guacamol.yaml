# @package _global_

defaults:
  - override /datamodule: guacamol

datamodule:
  batch_size: 256
  num_workers: 8

trainer:
  max_steps: 800000
  val_check_interval: 1000

sampling:
  num_samples: 512

model:
  model_name: llama-s
  attention_dropout: 0.5
