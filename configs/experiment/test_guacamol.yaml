# @package _global_

defaults:
  - override /datamodule: guacamol

datamodule:
  batch_size: 256

sampling:
  top_k: 5

model:
  model_name: llama-s
  pretrained_path: pretrained_models/train/Guacamol.ckpt
