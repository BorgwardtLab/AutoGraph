# @package _global_

defaults:
  - override /datamodule: sbm

datamodule:
  batch_size: 64

trainer:
  max_steps: 200000
  val_check_interval: 1000

model:
  model_name: llama2-s
  attention_dropout: 0.5
